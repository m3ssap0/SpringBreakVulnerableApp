<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.afs.vulnerable</groupId>
  <artifactId>SpringBreakVulnerableApp</artifactId>
  <version>1.0</version>
  <name>SpringBreakVulnerableApp</name>
  <description>This is a vulnerable application to test Spring Break exploit. Run it at your own risk!</description>
  <build>
    <sourceDirectory>src/main/java</sourceDirectory>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.7.0</version>
        <configuration>
          <release>10</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>1.5.6.RELEASE</version>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
      <version>1.5.6.RELEASE</version>
      <!-- 
         WARNING: This application contains serious security vulnerabilities. 
         Use an updated version of this dependency to prevent security issues. 
      -->
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-rest</artifactId>
      <version>1.5.6.RELEASE</version>
      <!-- 
         WARNING: This application contains serious security vulnerabilities. 
         Use an updated version of this dependency to prevent security issues. 
      -->
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
      <version>1.5.6.RELEASE</version>
      <!-- 
         WARNING: This application contains serious security vulnerabilities. 
         Use an updated version of this dependency to prevent security issues. 
      -->
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
      <version>1.5.6.RELEASE</version>
      <!--
        WARNING: This application contains serious security vulnerabilities.
        Use an updated version of this dependency to prevent security issues.
      -->
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>1.4.196</version>
    </dependency>
    <dependency>
      <!-- Needed for newer Java versions. -->
      <groupId>javax.xml.bind</groupId>
      <artifactId>jaxb-api</artifactId>
      <version>2.3.1</version>
    </dependency>
  </dependencies>
  <url>https://github.com/m3ssap0/SpringBreakVulnerableApp</url>
  <packaging>jar</packaging>
</project>